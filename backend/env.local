# 本地开发环境配置
NODE_ENV=development
PORT=3001

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=pixelwar3_dev_user
DB_PASSWORD=dev_password_123
DB_NAME=pixelwar3_dev

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379

# 本地验证模式 (设置为false使用真实Redis)
LOCAL_VALIDATION=false

# JWT 配置
JWT_SECRET=dev_jwt_secret_key_123
JWT_REFRESH_SECRET=dev_jwt_refresh_secret_key_456

# CORS 配置
CORS_ORIGIN=http://localhost:3000

# 其他配置
LOG_LEVEL=debug
